version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run: npm install
      - save_cache:
          key: "npm-packages"
          paths: -/src/node-modules
      - run: npm run lint
      - run:
          command: echo "failed step before"
          when: on_fail

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
